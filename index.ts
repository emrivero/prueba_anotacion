// a function that receives a string as parameter and returns the severity level according to the comparison of the string parameter with the values:"max", "min", "sum", "sum2", "occur", "avg", "max" if the string argument is null or undefined
function builder(string: string): AggregateOperation {
  return string;
}

// a function that receives a string as parameter and returns the severity level according to the comparison of the string parameter with the values:"max", "min", "sum", "sum2", "occur", "avg", "max" if the string argument is null or undefined
const builder_ = (string: string): AggregateOperation => {
  return string;
}
